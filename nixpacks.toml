[phases.setup]
aptPkgs = ["python3", "python3-pip", "gcc", "g++"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python 'ichimoku live.py'"
