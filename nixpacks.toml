[phases.setup]
nixPkgs = [
    "python311",
    "python311Packages.pip",
    "gcc",
    "pkg-config",
    "ta-lib",
    "cmake",
    "ninja"
]
cmds = [
    "python -V"
]

[phases.install]
cmds = [
    "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "python 'ichimoku live.py'"