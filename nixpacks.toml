[phases.setup]
cmds = [
    "apt-get update",
    "apt-get install -y --no-install-recommends python3 python3-pip build-essential wget pkg-config cmake"
]

[phases.install]
cmds = [
    "python3 -m pip install --upgrade pip",
    "wget http://prdownloads.sourceforge.net/ta-lib/ta-lib-0.4.0-src.tar.gz && tar -xzf ta-lib-0.4.0-src.tar.gz",
    "cd ta-lib/ && ./configure && make && make install && cd .. && rm -rf ta-lib*",
    "ldconfig",
    "python3 -m pip install --no-cache-dir numpy",
    "python3 -m pip install --no-cache-dir ta-lib",
    "python3 -m pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = "python3 'ichimoku live.py'"
